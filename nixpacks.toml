[phases.setup]
aptPkgs = ["supervisor"]

[phases.build]
cmds = [
    "npm ci --ignore-scripts",
    "npm run build"
]

[start]
cmd = "bash /app/start.sh"

[variables]
NIXPACKS_PHP_ROOT_DIR = "/app/public"
